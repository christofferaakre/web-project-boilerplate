import React from 'react';
import Loading from './Loading';
export const App = () => {
    return (
        <div>
            This is the root (App.js) component. Use it to render other components/routes or the loading component
        </div>
    );
};

export default App;
